export class AppConfig {
    public static NOTIFICATION_ERROR_OPTIONS = {
        timeOut: 5000,
        showProgressBar: true,
        clickToClose: true,
        lastOnBottom: true,
        position: ['top', 'center']
    };
}